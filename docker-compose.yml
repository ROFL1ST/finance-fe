services:

  frontend:

    build:
      context: .

    container_name: finance-fe

    restart: unless-stopped

    ports:
      - "5173:5173"

    environment:
      VITE_API_URL: http://localhost:8000/api

    volumes:
      - .:/app
      - /app/node_modules
